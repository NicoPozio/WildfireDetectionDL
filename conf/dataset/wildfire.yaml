paths:
  train: "/content/data/dataset/train"
  # Points to the PARENT folder containing 'wildfire' and 'nowildfire'
  synthetic: "/content/data/synthetic_dataset" 
  val: "/content/data/dataset/valid"
  test: "/content/data/dataset/test"

params:
  input_size: [224, 224]
  num_workers: 2
  use_synthetic: false  # Default to false, toggle via command line
  
  # ImageNet Statistics (Standard for ResNet)
  mean: [0.485, 0.456, 0.406]
  std: [0.229, 0.224, 0.225]
